server {
    listen 80;
    server_name myapp.django;

    location /static/ {
        alias /apt/myapp/static/;
    }

    location / {
        include proxy_params;
        proxy_pass http://unix:/run/gunicorn.sock;
    }
}